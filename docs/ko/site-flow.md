# Site-Flow 통합 가이드

> JiKiME-ADK와 Site-Flow 연동을 통한 프로젝트 관리 및 진행 상황 추적

## 개요

Site-Flow는 페이지 플로우 및 E2E 테스트 관리 플랫폼입니다. JiKiME-ADK와 연동하면 개발 과정에서 자동으로 프로젝트를 관리하고, 작업 내용을 기록하며, 진행 상황을 체크할 수 있습니다.

### Site-Flow 주요 기능

| 기능 | 설명 |
|------|------|
| **페이지 관리** | 사이트 페이지 구조 관리, 스크린샷 갤러리 |
| **기능(Feature) 관리** | 페이지별 기능 정의, 구현 상태 추적 |
| **캔버스(플로우)** | 페이지 간 연결 시각화, 사용자 흐름 관리 |
| **테스트 케이스** | E2E 테스트 생성, 실행, 배포 |
| **버그 리포트** | 버그/기능요청/개선 등록 및 추적 |
| **품질 분석** | 페이지 건강도, 영향 범위 분석 |

---

## 통합 아키텍처

```
┌─────────────────────────────────────────────────────────────────┐
│                        JiKiME-ADK                               │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │  Commands    │  │   Skills     │  │    Hooks     │          │
│  │ /jikime:*    │  │ jikime-*     │  │ Pre/Post     │          │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘          │
│         │                 │                 │                   │
│         └────────────┬────┴─────────────────┘                   │
│                      │                                          │
│              ┌───────▼───────┐                                  │
│              │ Site-Flow MCP │                                  │
│              │    Server     │                                  │
│              └───────┬───────┘                                  │
└──────────────────────┼──────────────────────────────────────────┘
                       │ HTTP API
                       ▼
┌──────────────────────────────────────────────────────────────────┐
│                        Site-Flow                                 │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐         │
│  │  Pages   │  │ Features │  │  Tests   │  │   Bugs   │         │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘         │
└──────────────────────────────────────────────────────────────────┘
```

---

## 설정

### 1. Site-Flow MCP 서버 설정

`.claude/settings.json`에 Site-Flow MCP 서버를 추가합니다:

```json
{
  "mcpServers": {
    "site-flow": {
      "command": "node",
      "args": ["./path/to/site-flow/mcp-server/dist/index.js"],
      "env": {
        "SITE_FLOW_API_URL": "http://localhost:5174",
        "SITE_FLOW_API_KEY": "sf_your_api_key_here"
      }
    }
  }
}
```

### 2. API 키 발급

Site-Flow에서 API 키를 발급받습니다:

1. Site-Flow 관리자 페이지 접속
2. 설정 → API 키 관리
3. 새 API 키 생성
4. `SITE_FLOW_API_KEY` 환경변수에 설정

---

## Commands

### `/jikime:project` - 프로젝트 관리

프로젝트를 등록하고 관리합니다.

```bash
# 프로젝트 초기화 (현재 디렉토리 분석)
/jikime:project init

# 페이지 목록 조회
/jikime:project list

# 페이지 등록
/jikime:project add "/login" "로그인 페이지"

# 페이지 상세 조회 (기능, 테스트, 버그 포함)
/jikime:project context "/login"
```

**사용하는 Site-Flow MCP 도구:**
- `list_pages` - 페이지 목록 조회
- `create_page` - 페이지 등록
- `update_page` - 페이지 수정
- `get_page_context` - 페이지 컨텍스트 조회

---

### `/jikime:progress` - 진행 상황 확인

전체 프로젝트 진행 상황을 확인합니다.

```bash
# 전체 통계
/jikime:progress

# 테스트 통계
/jikime:progress --test

# 버그 통계
/jikime:progress --bug

# 특정 페이지 건강도
/jikime:progress --page "/login"
```

**사용하는 Site-Flow MCP 도구:**
- `get_test_stats` - 테스트 통계
- `get_bug_stats` - 버그 통계
- `get_page_health` - 페이지 건강도
- `get_impact_scope` - 영향 범위 분석

---

### `/jikime:record` - 작업 기록

개발 과정에서 발생한 이슈나 변경사항을 기록합니다.

```bash
# 버그 등록
/jikime:record bug "로그인 실패 시 에러 메시지 없음" --page "/login"

# 기능 요청 등록
/jikime:record feature "소셜 로그인 추가" --page "/login"

# 개선 사항 등록
/jikime:record improvement "로그인 폼 UI 개선" --page "/login"

# 기능 상태 업데이트
/jikime:record update-feature FEATURE_ID --status completed
```

**사용하는 Site-Flow MCP 도구:**
- `create_bug_report` - 버그/기능요청/개선 등록
- `add_bug_comment` - 코멘트 추가
- `update_bug_report` - 상태 업데이트
- `update_feature` - 기능 상태 업데이트

---

## Skills

### `jikime-site-flow-project` - 프로젝트 관리 스킬

페이지, 기능, 캔버스를 관리하는 종합 스킬입니다.

```bash
# 스킬 호출
/jikime:skill site-flow-project

# 또는 직접 MCP 도구 사용
"Site Flow에서 현재 등록된 페이지 목록을 보여줘"
"로그인 페이지(/login)를 등록해줘"
"메인 플로우 캔버스를 생성해줘"
```

**포함 기능:**
- 페이지 CRUD (생성, 조회, 수정, 삭제)
- 기능 CRUD
- 캔버스 관리 (페이지 배치, 링크 연결)
- 페이지 이미지 갤러리 관리

---

### `jikime-site-flow-test` - 테스트 관리 스킬

E2E 테스트 케이스를 생성하고 관리하는 스킬입니다.

```bash
# 스킬 호출
/jikime:skill site-flow-test

# 또는 직접 MCP 도구 사용
"로그인 기능에 대한 E2E 테스트 케이스를 생성해줘"
"생성된 테스트 케이스를 파일로 배포해줘"
"테스트를 실행해줘"
```

**포함 기능:**
- 테스트 케이스 생성/조회
- 기능 기반 테스트 자동 생성 (`generate_test_cases`)
- 테스트 파일 배포 (`deploy_test_cases`)
- 테스트 실행 (`run_tests`)
- 테스트 실행 이력 조회

---

### `jikime-site-flow-bug` - 버그 리포트 스킬

버그, 기능 요청, 개선 사항을 관리하는 스킬입니다.

```bash
# 스킬 호출
/jikime:skill site-flow-bug

# 또는 직접 MCP 도구 사용
"로그인 페이지에 버그를 등록해줘"
"버그에 수정 코멘트를 추가해줘"
"버그 통계를 보여줘"
```

**포함 기능:**
- 버그/기능요청/개선 등록
- 상태 관리 (open → in-progress → resolved → closed)
- 코멘트 추가 (소스코드 첨부 지원)
- 통계 및 분석

---

## Hooks 자동화

JiKiME-ADK Hooks를 통해 개발 과정을 자동으로 Site-Flow에 기록합니다.

### PreCommit Hook

커밋 전 변경된 파일 기반으로 관련 페이지를 식별합니다.

```yaml
# .claude/settings.local.yaml
hooks:
  PreCommit:
    - command: "jikime-adk hooks site-flow pre-commit"
      description: "Site-Flow 페이지 연결 확인"
```

### PostCommit Hook

커밋 후 작업 내용을 Site-Flow에 자동 기록합니다.

```yaml
hooks:
  PostCommit:
    - command: "jikime-adk hooks site-flow post-commit"
      description: "Site-Flow 작업 기록"
```

### PostSpecComplete Hook

SPEC 완료 시 테스트 케이스를 자동 생성합니다.

```yaml
hooks:
  PostSpecComplete:
    - command: "jikime-adk hooks site-flow generate-tests"
      description: "Site-Flow 테스트 케이스 생성"
```

---

## 워크플로우 예시

### 1. 신규 프로젝트 시작

```bash
# 1. 프로젝트 초기화
/jikime:project init

# 2. 페이지 구조 등록
/jikime:project add "/login" "로그인 페이지"
/jikime:project add "/dashboard" "대시보드"
/jikime:project add "/settings" "설정 페이지"

# 3. 플로우 캔버스 생성
"메인 사용자 플로우 캔버스를 생성하고, 로그인 → 대시보드 → 설정 흐름을 연결해줘"
```

### 2. SPEC 기반 개발

```bash
# 1. SPEC 계획
/jikime:1-plan "로그인 기능"
  → Site-Flow에 Feature 자동 등록 (status: planned)

# 2. SPEC 구현
/jikime:2-run SPEC-LOGIN-001
  → Feature status: in-progress

# 3. SPEC 완료
/jikime:3-sync SPEC-LOGIN-001
  → Feature status: completed
  → 테스트 케이스 자동 생성
```

### 3. 진행 상황 모니터링

```bash
# 전체 진행 상황 확인
/jikime:progress

# 출력 예시:
# ┌─────────────────────────────────────┐
# │ 📊 프로젝트 진행 현황               │
# ├─────────────────────────────────────┤
# │ 페이지: 15개 (완료: 8, 진행중: 5)   │
# │ 기능: 45개 (완료: 30, 진행중: 10)   │
# │ 테스트: 120개 (통과: 95, 실패: 5)   │
# │ 버그: 12개 (해결: 8, 진행중: 3)     │
# └─────────────────────────────────────┘
```

### 4. 버그 발견 및 수정

```bash
# 1. 버그 등록
/jikime:record bug "로그인 실패 시 에러 메시지가 표시되지 않음" --page "/login" --severity major

# 2. 버그 수정 후 코멘트 추가
"BUG_ID 버그에 수정 코멘트를 추가해줘. 수정 내용: LoginForm.tsx에서 에러 핸들링 추가"

# 3. 버그 상태 업데이트
/jikime:record update-bug BUG_ID --status resolved
```

---

## Site-Flow MCP 도구 전체 목록

### 페이지 관리
| 도구 | 설명 |
|------|------|
| `list_pages` | 페이지 목록 조회 |
| `create_page` | 페이지 등록 |
| `update_page` | 페이지 수정 |
| `update_pages_bulk` | 여러 페이지 일괄 등록/수정 |
| `get_page_image` | 페이지 스크린샷 조회 |
| `get_page_context` | 페이지 컨텍스트 (기능, 테스트, 버그 포함) |

### 페이지 이미지 갤러리
| 도구 | 설명 |
|------|------|
| `list_page_images` | 페이지 이미지 목록 |
| `add_page_image` | 이미지 추가 |
| `delete_page_image` | 이미지 삭제 |

### 기능(Feature) 관리
| 도구 | 설명 |
|------|------|
| `list_features` | 기능 목록 조회 |
| `create_feature` | 기능 등록 |
| `update_feature` | 기능 수정 |

### 캔버스(플로우) 관리
| 도구 | 설명 |
|------|------|
| `list_canvases` | 캔버스 목록 |
| `create_canvas` | 캔버스 생성 |
| `get_canvas_pages` | 캔버스 페이지/연결 정보 |
| `add_page_to_canvas` | 캔버스에 페이지 추가 |
| `add_link_between_pages` | 페이지 간 연결 추가 |

### 테스트 케이스
| 도구 | 설명 |
|------|------|
| `list_test_cases` | 테스트 케이스 목록 |
| `create_test_case` | 테스트 케이스 생성 |
| `generate_test_cases` | 기능 기반 자동 생성 |
| `deploy_test_cases` | 테스트 파일 배포 |

### 테스트 실행
| 도구 | 설명 |
|------|------|
| `list_test_executions` | 실행 이력 조회 |
| `run_tests` | 테스트 실행 |

### 버그 리포트
| 도구 | 설명 |
|------|------|
| `list_bug_reports` | 버그 목록 |
| `create_bug_report` | 버그/기능요청/개선 등록 |
| `update_bug_report` | 버그 수정 |
| `add_bug_comment` | 코멘트 추가 |

### 통계/분석
| 도구 | 설명 |
|------|------|
| `get_test_stats` | 테스트 통계 |
| `get_bug_stats` | 버그 통계 |
| `get_page_health` | 페이지 건강도 |
| `get_impact_scope` | 영향 범위 분석 |

---

## 관련 문서

- [Commands Reference](./commands.md)
- [Skills Catalog](./skills-catalog.md)
- [Hooks System](./hooks.md)
- [Memory System](./memory.md)
