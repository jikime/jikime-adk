# 사용법

## 화면 구성

```
┌─────────────────────────────────────────────────────┐
│  사이드바   │           메인 패널                     │
│             │  ┌─────────────────┬─────────────────┐ │
│  서버 선택  │  │                 │                 │ │
│  ──────     │  │   채팅 영역     │   파일 트리 /   │ │
│  프로젝트   │  │  (ChatInterface) │   에디터 /      │ │
│  목록       │  │                 │   터미널 / Git  │ │
│  ──────     │  │                 │                 │ │
│  세션 목록  │  └─────────────────┴─────────────────┘ │
└─────────────────────────────────────────────────────┘
```

---

## 서버 연결 상태 확인

사이드바 상단에 서버 아이콘과 함께 연결 상태가 표시됩니다.

| 표시 | 의미 |
|---|---|
| 🟢 초록 점 · `연결됨` | WebSocket 연결 정상 |
| ⚫ 회색 점 · `연결 중...` | 연결 시도 중 또는 끊김 |

연결이 끊기면 3초 후 자동으로 재연결을 시도합니다.

---

## 프로젝트 선택

사이드바의 프로젝트 목록에서 작업할 프로젝트를 선택합니다.

- 목록은 서버의 `~/.claude/projects/` 디렉터리에서 자동으로 읽어옵니다.
- 서버를 전환하면 해당 서버의 프로젝트 목록으로 자동 갱신됩니다.
- 프로젝트를 선택하면 이전 대화 세션 목록도 함께 표시됩니다.

---

## 채팅 (Claude와 대화)

### 메시지 전송

1. 하단 입력창에 메시지를 입력합니다.
2. `Enter` 또는 전송 버튼을 클릭합니다.
3. Claude가 스트리밍 방식으로 응답합니다.

### 권한 모드 선택

입력창 옆의 모드 셀렉터로 Claude의 작업 권한 범위를 설정합니다.

| 모드 | 설명 |
|---|---|
| `bypassPermissions` | 모든 도구 사용을 자동 승인 (root 환경에서는 `acceptEdits`로 자동 전환) |
| `acceptEdits` | 파일 편집 자동 승인, 기타 도구는 승인 요청 |
| `default` | 모든 도구 사용 전 브라우저에서 승인 요청 |

> **root 환경 주의**: root 또는 sudo 권한으로 실행된 서버에서는 `bypassPermissions`를 선택해도 자동으로 `acceptEdits`로 전환됩니다. (`--dangerously-skip-permissions` 옵션이 root에서 차단되는 Claude CLI 정책에 따름)

### 도구 사용 승인

`default` 모드에서 Claude가 도구(파일 읽기, 코드 실행 등)를 사용하려 할 때 브라우저에 승인 요청 팝업이 표시됩니다.

- **허용**: 도구 실행을 승인합니다.
- **거부**: 해당 도구 사용을 막습니다.
- 30초 내 응답이 없으면 자동으로 **허용** 처리됩니다.

### 대화 중단

응답 생성 중 **중지** 버튼을 클릭하면 현재 응답을 즉시 중단합니다.

### 세션 이어서 하기

사이드바에서 이전 세션을 선택하면 해당 대화를 이어서 진행할 수 있습니다.

---

## 파일 편집기

우측 패널의 **파일** 탭에서 프로젝트 파일을 탐색하고 편집합니다.

- 파일 트리에서 파일을 클릭하면 Monaco 에디터로 열립니다.
- 에디터 상단에 현재 열린 파일의 **전체 경로**가 표시됩니다.
- 편집 후 `Ctrl+S` / `Cmd+S` 로 저장합니다.

---

## 터미널

우측 패널의 **터미널** 탭에서 서버 쉘에 직접 접속합니다.

- 서버에서 `node-pty`가 정상 빌드된 경우에만 활성화됩니다.
- 터미널을 열면 **쉘 프롬프트**(bash/zsh)가 바로 표시됩니다. Claude CLI를 사용하려면 직접 `claude` 명령어를 입력하세요.
- 창 크기를 조절하면 터미널도 자동으로 리사이즈됩니다.

```bash
# 터미널에서 Claude CLI 직접 실행 예시
claude

# 특정 모드로 실행
claude --print "현재 디렉터리의 파일 목록을 알려줘"
```

---

## Git 패널

우측 패널의 **Git** 탭에서 현재 프로젝트의 Git 상태를 확인하고 커밋할 수 있습니다.

| 기능 | 설명 |
|---|---|
| 상태 조회 | `git status --short` 결과 표시 |
| 변경 사항 | `git diff --stat` 결과 표시 |
| 로그 | 최근 20개 커밋 표시 |
| 스테이징 | 파일 선택 후 `git add` |
| 커밋 | 메시지 입력 후 커밋 |
| 브랜치 | 브랜치 목록 및 체크아웃 |

---

## 모델 선택

채팅 입력창 옆에서 사용할 Claude 모델을 선택할 수 있습니다.

| 모델 | 특징 |
|---|---|
| `claude-sonnet-4-5` | 기본값. 속도와 성능 균형 |
| `claude-opus-4-5` | 최고 성능, 복잡한 작업에 적합 |
| `claude-haiku-4-5` | 빠른 응답, 간단한 작업에 적합 |
